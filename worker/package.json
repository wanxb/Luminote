{
  "name": "luminote-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --persist-to .wrangler/state/local-speed",
    "deploy": "wrangler deploy",
    "sync:local": "node scripts/sync-remote-to-local.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260403.1",
    "typescript": "5.7.2",
    "wrangler": "^4.37.0"
  }
}
