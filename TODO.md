# Luminote 待办

## P0

* 将 Lightbox 从占位结构升级为真实可交互组件
* 首页点击照片后调用 `/api/photos/:id`
* 在 Lightbox 中展示 EXIF、备注和标签
* 实现真实展示图生成，避免前台直接复用原图
* 实现真实水印展示图生成
* 将水印加载规则接入前台大图
* 为 Worker 绑定真实 D1 与 R2，并补齐本地开发说明

## P1

* 实现管理员会话从 Bearer token 升级为 HttpOnly Cookie
* 完善上传失败重试和错误提示
* 增加照片删除的真实数据删除链路
* 删除时同步移除 D1 记录和 R2 对象
* 补齐站点配置更新接口
* 支持站点标题、简介、水印文本配置

## P2

* 评估服务端 EXIF 方案，决定是否替换浏览器端提取
* 评估服务端图片处理方案，决定是否引入异步队列
* 考虑标签详情展示
* 考虑标签筛选和搜索
* 升级 Next.js 到安全修复版本

## 明日建议起点

* 优先做真实 Lightbox 和 `/api/photos/:id` 联调
* 完成后再进入展示图和水印图生成
